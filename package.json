{
  "name": "ai-success-story",
  "version": "0.1.0",
  "private": true,
  "description": "AI Success Story — micro-cases of AI agents getting things to work.",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
